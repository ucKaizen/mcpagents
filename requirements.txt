# --- Core ---
fastmcp>=0.1.3        # MCP server + client
openai>=1.40.0        # LLM API + tool calling
python-dotenv>=1.0.1  # loading .env

# --- Web agent ---
fastapi>=0.111.0      # FastAPI for web server + WebSocket
uvicorn>=0.30.0       # ASGI server
websockets>=12.0      # WebSocket support for FastAPI
pydantic>=2.7.0       # data validation

# --- SLM service (slm/) ---
flask
transformers
torch
requests
rapidfuzz